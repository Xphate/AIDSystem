<p id="notice"><%= notice %></p>

<p>欢迎, <%= current_user.username %></p>

<h1>用户中心</h1>
<h2>个人资料</h2>
<table>
  <thead>
  <tr>
    <th>用户名</th>
    <th>邮箱</th>
    <th>手机号码</th>
    <th>创建时间</th>
    <th>用户状态</th>
    <th>是否管理员</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
      <tr>
        <td><%= @user.username %></td>
        <td><%= @user.email %></td>
        <td><%= @user.phonenumber %></td>
        <td><%= @user.created_at %></td>
        <td>正常</td>
        <td><%= @user.admin %></td>
        <td><%= link_to '编辑', edit_user_edit_path %></td>
        <td><%= link_to '修改密码',edit_registration_path(:user) %></td>
      </tr>
  </tbody>
</table>

<h2>组织机构和工作职务</h2>
<%= @user.job %>

<h2>登录历史</h2>
<p>本次登录时间： <%=@user.current_sign_in_at %></p>
<p>上次登录时间： <%=@user.last_sign_in_at %></p>
<p>登录次数： <%=@user.sign_in_count %></p>
<p>登录ip: <%=@user.current_sign_in_ip %></p>

<br>

